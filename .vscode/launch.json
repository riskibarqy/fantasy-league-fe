{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Open app in Chrome (Vite)",
      "type": "pwa-chrome",
      "request": "launch",
      "url": "http://localhost:5173",
      "webRoot": "${workspaceFolder}"
    }
  ]
}
